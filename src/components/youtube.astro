---
import youtube from "./Youtube.svelte";
export interface Props {
  videoId: string;
  componentParams?: string;
}
const { videoId, componentParams } = Astro.props;
---

<youtube {videoId} {componentParams} client:visible></youtube>
